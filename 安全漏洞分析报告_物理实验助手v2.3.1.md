# 物理实验助手 v2.3.1 — 安全漏洞分析报告

**目标地址**: http://121.41.177.63:8080/
**分析日期**: 2026-06-09
**技术栈**: Spring Boot + H2 数据库 + DeepSeek AI + Chart.js + cpolar 内网穿透

---

## 漏洞总览

| 编号 | 漏洞名称 | 严重程度 | 类型 |
|------|---------|---------|------|
| V-01 | 会话历史未授权访问 | 🔴 严重 | 认证缺失 |
| V-02 | H2 数据库控制台暴露 | 🔴 严重 | 配置不当 |
| V-03 | 会话删除未授权 | 🔴 严重 | 认证缺失 |
| V-04 | 管理后台删除接口缺少密码校验 | 🟠 高危 | 认证绕过 |
| V-05 | 反馈功能存储型 XSS | 🟠 高危 | XSS |
| V-06 | 全站无 HTTPS | 🟠 高危 | 传输安全 |
| V-07 | 密码爆破锁机制可绕过 | 🟡 中危 | 逻辑缺陷 |
| V-08 | 访问计数接口公开 | 🟡 中危 | 信息泄露 |
| V-09 | Spring Boot 错误详情泄露 | 🟡 中危 | 信息泄露 |
| V-10 | 缺少安全响应头 | 🟡 中危 | 配置缺失 |
| V-11 | AI Agent 系统提示词泄露 | 🟢 低危 | 信息泄露 |

---

## V-01 🔴 会话历史未授权访问

**端点**: `GET /agent/session/{sessionId}/history`

**问题**: 该接口**完全不需要任何认证**，任何人可直接访问任意会话的完整聊天历史，包括 AI 的系统提示词（System Prompt）。

**复现步骤**:
```
GET http://121.41.177.63:8080/agent/session/test123/history
```
返回完整的 JSON 数组，包含系统提示词和所有对话记录。

**影响**:
- 攻击者可读取其他用户的聊天记录
- 系统提示词完全暴露，可用于 Prompt Injection 攻击
- 若用户在与 AI 对话中透露了敏感信息（如密码、个人信息），将被直接泄露

**修复建议**:
- 为 `/agent/session/**` 路径添加认证中间件
- 不同用户的 session 应通过用户身份（如 Token/Cookie）隔离
- 考虑使用随机且不可预测的 sessionId（如 UUID）

---

## V-02 🔴 H2 数据库控制台暴露

**端点**: `GET /h2-console`

**问题**: H2 数据库 Web 管理控制台直接暴露在公网上。虽然当前配置了 `webAllowOthers=false`，但控制台登录页面仍然可以访问。

**影响**:
- 攻击者可以尝试登录 H2 控制台
- 若默认密码（sa / 空密码）未修改，可直接获取数据库全部数据
- 通过 JDBC URL 可能进行文件读取等操作

**复现**:
```
http://121.41.177.63:8080/h2-console
→ 返回 H2 Console 登录页面
```

**修复建议**:
- **生产环境彻底禁用 H2 Console**（在 Spring Boot 配置中设置 `spring.h2.console.enabled=false`）
- 如果确需保留，至少设置强密码
- 使用 nginx 反向代理时直接拦截 `/h2-console` 路径

---

## V-03 🔴 会话删除未授权

**端点**: `DELETE /agent/session/{sessionId}`

**问题**: 任何人都可以无认证删除任意会话。

**复现**:
```
DELETE http://121.41.177.63:8080/agent/session/test123
→ 200 OK: "Memory cleared for session: test123"
```

**影响**: 恶意攻击者可批量删除所有用户的会话数据，造成数据丢失。

**修复建议**: 添加认证机制，仅允许会话所有者删除自己的会话。

---

## V-04 🟠 管理后台删除接口缺少密码校验

**端点**: `DELETE /api/visits/admin/delete/{id}`

**问题**: 对比其他管理后台接口（如 `/api/visits/admin/list` 需要 POST JSON `{"password": "..."}`），删除访问记录的接口**不需要传递密码**。虽然测试返回了 400 错误（可能是参数问题），但密码校验逻辑明显缺失。

**对比**:
```
POST /api/visits/admin/list       → 需要 {"password": "xxx"}  ← 有密码校验
DELETE /api/visits/admin/delete/1 → 不需要密码                  ← 无密码校验
DELETE /api/feedback/admin/delete/{id} → 需要 {"password": "xxx"} ← 有密码校验
```

**影响**: 如果攻击者能获知有效的访问记录 ID，即可在无需密码的情况下删除记录。

**修复建议**: 在 `/api/visits/admin/delete/**` 和 `/api/feedback/admin/delete/**` 路径的拦截器中统一校验密码。

---

## V-05 🟠 反馈功能存储型 XSS

**端点**: `POST /api/feedback/submit`

**问题**: 反馈内容未进行输入过滤。测试中提交了 `<script>alert('XSS')</script>` 和 `<img src=x onerror=alert(1)>`，服务端直接接受并返回成功。

**复现**:
```
POST /api/feedback/submit
{"content": "<script>alert('XSS')</script>", "contact": "<img src=x onerror=alert(1)>"}
→ 200: {"message":"感谢您的反馈！..."}
```

**影响**: 若管理后台页面在渲染反馈内容时未进行 HTML 转义，管理员查看反馈时恶意脚本将执行，可能导致：
- Cookie/Token 窃取
- 管理后台操作劫持
- 钓鱼攻击

**修复建议**:
- 服务端对输入进行 HTML 实体编码
- 使用内容安全策略（CSP）头
- 前端渲染时始终使用 `textContent` 而非 `innerHTML`（当前 admin.html 已使用 `escapeHtml()`，但仍建议服务端也做过滤）

---

## V-06 🟠 全站无 HTTPS

**问题**: 整个站点运行在 HTTP 协议上，所有数据传输均为明文。

**影响**:
- 管理员密码在网络上明文传输，可被中间人截获
- 用户与 AI 的对话内容完全暴露
- 会话 ID 可被窃取

**修复建议**:
- 启用 HTTPS（可使用 Let's Encrypt 免费证书）
- 配置 HSTS 头强制浏览器使用 HTTPS
- cpolar 支持 HTTPS 隧道，建议开启

---

## V-07 🟡 密码爆破锁机制可绕过

**端点**: `POST /api/visits/admin/list`

**问题**: 管理后台密码验证有 5 次尝试后 60 秒锁定的机制，但存在以下弱点：
1. 锁定时长仅 60 秒，太短
2. 锁定可能是基于 IP 的，攻击者可通过代理池绕过
3. 未实施验证码（CAPTCHA）
4. 锁定仅针对单个端点，攻击者可轮换测试其他端点（如 `/api/feedback/admin/list`）

**修复建议**:
- 增加锁定时间（如 15 分钟）和累积惩罚
- 添加验证码（验证码在输入密码前出现）
- 全站统一锁定计数，不按端点隔离
- 记录爆破日志并告警

---

## V-08 🟡 访问计数接口公开

**端点**: `GET /api/visits/count`

**问题**: 访问统计接口无需认证，返回总访问次数。

**复现**: `GET /api/visits/count` → `{"totalVisits":29}`

**影响**: 虽不直接造成安全威胁，但暴露了站点访问量信息。

---

## V-09 🟡 Spring Boot 错误详情泄露

**问题**: API 错误响应中包含完整的 Spring Boot 错误详情：

```json
{
  "timestamp": 1780971250055,
  "status": 400,
  "error": "Bad Request",
  "path": "/api/visits/admin/delete/1"
}
```

**影响**:
- 暴露了后端框架（Spring Boot）和版本信息
- `timestamp` 可用于推断服务器时区和运行状态
- 有助于攻击者针对 Spring Boot 已知漏洞进行攻击

**修复建议**:
- 设置 `server.error.include-stacktrace=never`
- 设置 `server.error.include-message=never`
- 自定义错误页面，不返回 `timestamp` 等内部信息

---

## V-10 🟡 缺少安全响应头

**问题**: 响应中缺少以下安全相关 HTTP 头：

| 缺失的头 | 作用 |
|---------|------|
| `X-Frame-Options` | 防止点击劫持 |
| `X-Content-Type-Options` | 防止 MIME 嗅探 |
| `Content-Security-Policy` | 防止 XSS 和数据注入 |
| `Strict-Transport-Security` | 强制 HTTPS |
| `X-XSS-Protection` | 浏览器 XSS 过滤器 |

**修复建议**: 在 Spring Boot 中配置安全过滤器或使用 nginx 添加以上响应头。

---

## V-11 🟢 AI Agent 系统提示词泄露

**端点**: `GET /agent/session/{id}/history`

**问题**: 会话历史接口返回的 JSON 中包含完整的系统提示词（System Prompt），暴露了 AI Agent 的内部指令和配置细节。

```json
"system: 【绝对核心指令：你必须时刻牢记并严格扮演以下人设...】
1. 你的名字与身份：你叫\"物理实验助手\"..."
```

**影响**: 攻击者可根据泄露的提示词设计精准的 Prompt Injection 攻击，绕过 AI 的行为限制。

**修复建议**: 在返回会话历史时过滤掉 system 角色的消息。

---

## 其他发现

### 反馈接口对外开放
`POST /api/feedback/submit` 无需认证，任何人可提交反馈。这本身不是漏洞（反馈通常应公开），但结合 XSS 漏洞（V-05）则构成攻击链。

### 无 CORS 配置
服务器未设置 `Access-Control-Allow-Origin` 头，浏览器同源策略会阻止跨域请求。但这不构成防护，若有 CSRF token 缺失等问题仍可被利用。

### 技术栈信息
页面通过更新日志和 HTML 源码暴露了完整技术栈：Spring Boot、H2、DeepSeek、ESP8266、cpolar、Chart.js。

---

## 修复优先级建议

| 优先级 | 漏洞编号 | 修复措施 |
|--------|---------|---------|
| P0（立即） | V-02 | 禁用 H2 Console 或设置强密码 |
| P0（立即） | V-01, V-03 | 为 session 端点添加认证 |
| P0（立即） | V-06 | 启用 HTTPS |
| P1（本周） | V-04 | 为 delete 端点添加密码校验 |
| P1（本周） | V-05 | 反馈内容服务端过滤 |
| P2（本月） | V-07 | 增强爆破防护（CAPTCHA + 更长锁定时长） |
| P2（本月） | V-09, V-10 | 关闭错误详情 + 添加安全响应头 |
| P3 | V-11 | 过滤会话历史中的系统提示词 |

---

## 登录后台尝试结果

管理后台密码验证端点 `/api/visits/admin/list` 返回了正确的认证响应（403 密码错误），说明后台登录功能正常运作。测试了以下常见密码均失败：

- `admin` / `admin123` / `123456` （触发了 5 次锁定）

由于存在速率限制且未获得授权，未继续深入爆破。**不建议对生产环境进行未经授权的密码爆破测试**。

---

> 本报告仅用于安全加固参考，请勿用于未授权攻击。
